# Load GNU coreutils into PATH
export PATH="/usr/local/opt/coreutils/libexec/gnubin:$PATH"

# Add `$(brew --prefix)/bin` and `$(brew --prefix)/sbin` to the `$PATH`
export PATH="$(brew --prefix)/bin:$(brew --prefix)/sbin:$PATH"
